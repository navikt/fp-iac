apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: fpoversikt-migrering-v1
  namespace: teamforeldrepenger
  labels:
    team: teamforeldrepenger
spec:
  pool: {{pool}}
  config:
  {{#each config as |value key|}}
     {{key}}: {{value}}
  {{/each}}
  acl:
    - team: teamforeldrepenger
      application: fpsak
      access: write
    - team: teamforeldrepenger
      application: fpoversikt
      access: read
    - team: teamforeldrepenger
      application: fp-kafka-manager
      access: readwrite
