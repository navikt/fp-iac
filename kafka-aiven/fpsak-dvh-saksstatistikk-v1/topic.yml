apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: fpsak-dvh-saksstatistikk-v1
  namespace: teamforeldrepenger
  labels:
    team: teamforeldrepenger
spec:
  pool: {{pool}}
  config:
  {{#each config as |value key|}}
     {{key}}: {{value}}
  {{/each}}
  acl:
    - team: teamforeldrepenger
      application: fpsak
      access: write
  {{#each acl}}
    - team: {{team}}
      application: {{app}}
      access: {{access}}
  {{/each}}

